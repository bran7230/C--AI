#include <iostream>
#include <cmath>
#include <vector>
#include "Adamath.h"
#include "gui.cpp"
#include <windows.h>
//Use for gui tests
/*
HINSTANCE hInstance = GetModuleHandle(NULL);
LaunchGui(hInstance, SW_SHOW);
*/
int main()
{


    HINSTANCE hInstance = GetModuleHandle(NULL);
    LaunchGui(hInstance, SW_SHOW);



    return 0;
}
